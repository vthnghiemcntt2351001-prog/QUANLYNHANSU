@{
    ViewData["Title"] = "Quản lý người dùng";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    /* --- KHU VỰC CSS CẢI TIẾN --- */
    body {
        background-color: #f0f2f5; /* Nền xám nhạt hiện đại */
        font-family: 'Poppins', 'Segoe UI', sans-serif; /* Sử dụng font hiện đại hơn */
    }

    .container.mt-5 {
        padding-top: 30px;
    }

    .page-header {
        margin-bottom: 25px;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .page-header h2 {
        color: #172b4d; /* Màu chữ đậm cho tiêu đề */
        font-weight: 700;
        border-left: 5px solid #0d6efd; /* Đường viền nhấn mạnh */
        padding-left: 15px;
    }

    .filter-bar {
        background-color: white;
        padding: 15px;
        border-radius: 12px;
        margin-bottom: 20px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05); /* Shadow nhẹ nhàng */
        display: flex;
        gap: 15px;
        align-items: center;
    }

    .user-table th {
        background-color: #0d6efd; /* Màu header chính */
        color: white;
        text-align: center;
        font-weight: 600;
        vertical-align: middle;
        white-space: nowrap; /* Ngăn tiêu đề bị xuống dòng */
    }

    .user-table td {
        vertical-align: middle;
        text-align: center;
        padding: 12px 8px; /* Tăng padding */
    }

    .user-table tr:hover {
        background-color: #e6f7ff; /* Hover màu xanh nhạt hơn */
        cursor: pointer;
    }

    /* Nút Thêm mới */
    .btn-add {
        background: linear-gradient(45deg, #0d6efd, #007bff);
        color: white;
        border: none;
        border-radius: 50px; /* Nút tròn hơn */
        padding: 10px 20px;
        font-weight: 600;
        transition: all 0.3s ease;
    }

    .btn-add:hover {
        transform: scale(1.05);
        box-shadow: 0 8px 15px rgba(13, 110, 253, 0.4);
    }

    /* Modal Styling */
    .modal-content {
        border-radius: 15px;
        border: none;
    }

    .modal-header {
        background: #0d6efd; /* Đơn giản hóa background modal */
        color: white;
        border-radius: 15px 15px 0 0;
        border-bottom: none;
    }

    .btn-submit {
        background: #28a745; /* Màu Xanh lá (Success) cho nút tạo mới */
        color: white;
        border: none;
        width: 100%;
        padding: 10px;
        border-radius: 8px;
        transition: 0.3s;
    }

    .btn-submit:hover {
        background: #218838;
    }

    /* Toast notification */
    .toast-container {
        position: fixed;
        top: 20px;
        right: 20px;
        z-index: 2000;
    }

    .toast {
        border-left: 6px solid;
        box-shadow: 0 5px 15px rgba(0,0,0,0.15);
        border-radius: 10px;
    }

    .toast-success {
        border-left-color: #28a745; /* Xanh lá */
    }

    .toast-danger {
        border-left-color: #dc3545; /* Đỏ */
    }

    .toast-warning {
        border-left-color: #ffc107; /* Vàng */
    }

    /* Nút Thao tác trong bảng */
    .action-btn {
        margin: 0 3px;
        padding: 4px 8px;
        border-radius: 6px;
        font-size: 0.85rem;
    }
</style>

<div class="container mt-5">
    <div class="page-header">
        <h2><i class="fas fa-users"></i> Quản lý người dùng</h2>
        <button class="btn btn-add" data-bs-toggle="modal" data-bs-target="#modalThemNguoiDung">
            <i class="fas fa-plus-circle"></i> Thêm người dùng mới
        </button>
    </div>

    <div class="filter-bar">
        <label for="roleFilter" class="form-label mb-0 fw-bold text-muted">Lọc theo Vai trò:</label>
        <select id="roleFilter" class="form-select" style="width: 180px;">
            <option value="all">Tất cả</option>
            <option value="Admin">Admin</option>
            <option value="Quản lý">Quản lý</option>
            <option value="Nhân viên">Nhân viên</option>
        </select>
        <div class="input-group" style="width: 250px;">
            <span class="input-group-text"><i class="fas fa-search"></i></span>
            <input type="text" id="searchInput" class="form-control" placeholder="Tìm theo tên hoặc email...">
        </div>
    </div>

    <div class="card shadow-lg border-0" style="border-radius: 12px;">
        <div class="table-responsive">
            <table class="table table-hover user-table" id="userTable">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Tên đăng nhập</th>
                        <th>Email</th>
                        <th>Mật khẩu</th>
                        <th><i class="fas fa-user-tag"></i> Vai trò</th>
                        <th><i class="fas fa-clock"></i> Truy cập gần nhất</th>
                        <th>Thao tác</th>
                    </tr>
                </thead>
                <tbody id="userList">
                    <tr>
                        <td><input type="checkbox" /></td>
                        <td>admin01</td>
                        <td>admin@ntv.vn</td>
                        <td>••••••</td>
                        <td><span class="badge bg-primary">Admin</span></td>
                        <td>2025-11-06 18:30</td>
                        <td>
                            <button class="btn btn-warning action-btn" onclick="suaNguoiDung(this)"><i class="fas fa-edit"></i> Sửa</button>
                            <button class="btn btn-danger action-btn" onclick="xoaNguoiDung(this)"><i class="fas fa-trash-alt"></i> Xóa</button>
                        </td>
                    </tr>
                    <tr>
                        <td><input type="checkbox" /></td>
                        <td>nv001</td>
                        <td>nhanvien@ntv.vn</td>
                        <td>••••••</td>
                        <td><span class="badge bg-secondary">Nhân viên</span></td>
                        <td>2025-11-06 17:20</td>
                        <td>
                            <button class="btn btn-warning action-btn" onclick="suaNguoiDung(this)"><i class="fas fa-edit"></i> Sửa</button>
                            <button class="btn btn-danger action-btn" onclick="xoaNguoiDung(this)"><i class="fas fa-trash-alt"></i> Xóa</button>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="toast-container" id="toastContainer"></div>

<div class="modal fade" id="modalThemNguoiDung" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content shadow">
            <div class="modal-header">
                <h5 class="modal-title fw-bold"><i class="fas fa-user-plus"></i> Thêm người dùng mới</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <form id="formThemNguoiDung">
                    <div class="mb-3">
                        <label class="form-label">Tên đăng nhập</label>
                        <input type="text" class="form-control" id="username" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Email</label>
                        <input type="email" class="form-control" id="email" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Mật khẩu</label>
                        <input type="password" class="form-control" id="password" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Phân quyền</label>
                        <select class="form-select" id="role">
                            <option>Nhân viên</option>
                            <option>Quản lý</option>
                            <option>Admin</option>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-submit mt-3">Tạo tài khoản</button>
                </form>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="modalSuaNguoiDung" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content shadow">
            <div class="modal-header">
                <h5 class="modal-title fw-bold"><i class="fas fa-user-edit"></i> Chỉnh sửa người dùng</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <form id="formSuaNguoiDung">
                    <div class="mb-3">
                        <label class="form-label">Tên đăng nhập</label>
                        <input type="text" class="form-control" id="editUsername" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Email</label>
                        <input type="email" class="form-control" id="editEmail" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Mật khẩu</label>
                        <input type="text" class="form-control" id="editPassword" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Vai trò</label>
                        <select class="form-select" id="editRole">
                            <option>Nhân viên</option>
                            <option>Quản lý</option>
                            <option>Admin</option>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-submit mt-3">Lưu thay đổi</button>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
    let rowDangSua = null;

    // 🔍 Tìm kiếm
    document.getElementById("searchInput").addEventListener("input", function () {
        const keyword = this.value.toLowerCase();
        document.querySelectorAll("#userList tr").forEach(row => {
            const name = row.cells[1].textContent.toLowerCase();
            const email = row.cells[2].textContent.toLowerCase();
            row.style.display = name.includes(keyword) || email.includes(keyword) ? "" : "none";
        });
    });

    // 🧩 Lọc vai trò
    document.getElementById("roleFilter").addEventListener("change", function () {
        const filter = this.value;
        document.querySelectorAll("#userList tr").forEach(row => {
            const role = row.cells[4].textContent.trim().replace(/<\/?span[^>]*>/g, '').trim(); // Loại bỏ badge span khi so sánh
            row.style.display = (filter === "all" || role === filter) ? "" : "none";
        });
    });

    // 🟢 Thêm người dùng
    document.getElementById("formThemNguoiDung").addEventListener("submit", function (e) {
        e.preventDefault();
        const username = document.getElementById("username").value;
        const email = document.getElementById("email").value;
        const password = document.getElementById("password").value;
        const role = document.getElementById("role").value;
        const now = new Date().toLocaleString('vi-VN');

        let badgeClass = '';
        if (role === 'Admin') badgeClass = 'bg-primary';
        else if (role === 'Quản lý') badgeClass = 'bg-info text-dark';
        else badgeClass = 'bg-secondary';

        const newRow = `
            <tr>
                <td><input type="checkbox" /></td>
                <td>${username}</td>
                <td>${email}</td>
                <td>${password}</td>
                <td><span class="badge ${badgeClass}">${role}</span></td>
                <td>${now}</td>
                <td>
                    <button class="btn btn-warning action-btn" onclick="suaNguoiDung(this)"><i class="fas fa-edit"></i> Sửa</button>
                    <button class="btn btn-danger action-btn" onclick="xoaNguoiDung(this)"><i class="fas fa-trash-alt"></i> Xóa</button>
                </td>
            </tr>`;
        document.getElementById("userList").insertAdjacentHTML("beforeend", newRow);

        bootstrap.Modal.getInstance(document.getElementById('modalThemNguoiDung')).hide();
        this.reset();
        showToast("✅ Đã thêm người dùng mới!", "success");
    });

    // ✏️ Sửa
    function suaNguoiDung(btn) {
        rowDangSua = btn.closest("tr");
        document.getElementById("editUsername").value = rowDangSua.cells[1].textContent;
        document.getElementById("editEmail").value = rowDangSua.cells[2].textContent;
        document.getElementById("editPassword").value = rowDangSua.cells[3].textContent;
        // Lấy nội dung text của badge (Vai trò)
        const currentRole = rowDangSua.cells[4].querySelector('.badge') ? rowDangSua.cells[4].querySelector('.badge').textContent.trim() : rowDangSua.cells[4].textContent.trim();
        document.getElementById("editRole").value = currentRole;
        new bootstrap.Modal(document.getElementById('modalSuaNguoiDung')).show();
    }

    // 💾 Lưu thay đổi
    document.getElementById("formSuaNguoiDung").addEventListener("submit", function (e) {
        e.preventDefault();
        const newRole = document.getElementById("editRole").value;
        let badgeClass = '';
        if (newRole === 'Admin') badgeClass = 'bg-primary';
        else if (newRole === 'Quản lý') badgeClass = 'bg-info text-dark';
        else badgeClass = 'bg-secondary';

        rowDangSua.cells[1].textContent = document.getElementById("editUsername").value;
        rowDangSua.cells[2].textContent = document.getElementById("editEmail").value;
        rowDangSua.cells[3].textContent = document.getElementById("editPassword").value;
        rowDangSua.cells[4].innerHTML = `<span class="badge ${badgeClass}">${newRole}</span>`; // Cập nhật cả badge
        rowDangSua.cells[5].textContent = new Date().toLocaleString('vi-VN');

        bootstrap.Modal.getInstance(document.getElementById('modalSuaNguoiDung')).hide();
        showToast("✏️ Cập nhật thông tin người dùng thành công!", "warning");
    });

    // 🗑️ Xóa
    function xoaNguoiDung(btn) {
        if (confirm("Bạn có chắc muốn xóa người dùng này không? Hành động này không thể hoàn tác.")) {
            btn.closest("tr").remove();
            showToast("🗑️ Đã xóa người dùng!", "danger");
        }
    }

    // 🔔 Toast
    function showToast(message, type) {
        const toastId = "toast" + Date.now();
        const bgClass = type === "success" ? "toast-success" :
                        type === "warning" ? "toast-warning" :
                        type === "danger" ? "toast-danger" : "";
        const toastHTML = `
        <div class="toast align-items-center text-dark bg-white border-0 ${bgClass}" id="${toastId}" role="alert">
            <div class="d-flex">
                <div class="toast-body">${message}</div>
                <button type="button" class="btn-close me-2 m-auto" data-bs-dismiss="toast"></button>
            </div>
        </div>`;
        document.getElementById("toastContainer").insertAdjacentHTML("beforeend", toastHTML);
        const toastEl = document.getElementById(toastId);
        const toast = new bootstrap.Toast(toastEl, { delay: 3000 });
        toast.show();
        toastEl.addEventListener('hidden.bs.toast', () => toastEl.remove());
    }
</script>